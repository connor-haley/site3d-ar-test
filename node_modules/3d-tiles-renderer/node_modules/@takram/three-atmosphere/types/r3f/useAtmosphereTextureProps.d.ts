import { PrecomputedTextures } from '../PrecomputedTexturesLoader';

export declare function useAtmosphereTextureProps(url: string, useHalfFloat?: boolean): {
    textures: PrecomputedTextures;
    useHalfFloat: boolean;
};
