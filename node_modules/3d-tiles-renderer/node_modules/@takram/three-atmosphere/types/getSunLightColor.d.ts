import { AtmosphereParameters } from './AtmosphereParameters';
import { Ellipsoid } from '@takram/three-geospatial';
import { Color, Vector3, DataTexture } from 'three';

export interface SunLightColorOptions {
    ellipsoid?: Ellipsoid;
    correctAltitude?: boolean;
    photometric?: boolean;
}
export declare function getSunLightColor(transmittanceTexture: DataTexture, worldPosition: Vector3, sunDirection: Vector3, result?: Color, { ellipsoid, correctAltitude, photometric }?: SunLightColorOptions, atmosphere?: AtmosphereParameters): Color;
