import { SkyMaterial } from '../SkyMaterial';
import { AtmosphereMaterialProps } from '../AtmosphereMaterialBase';
import { BufferGeometry, Mesh, Vector3 } from 'three';
import { MeshProps } from '@react-three/fiber';

export type SkyImpl = Mesh<BufferGeometry, SkyMaterial>;
export interface SkyProps extends MeshProps, AtmosphereMaterialProps {
    sun?: boolean;
    moon?: boolean;
    moonDirection?: Vector3;
    moonAngularRadius?: number;
    lunarRadianceScale?: number;
}
export declare const Sky: import('react').ForwardRefExoticComponent<Omit<SkyProps, "ref"> & import('react').RefAttributes<SkyImpl>>;
