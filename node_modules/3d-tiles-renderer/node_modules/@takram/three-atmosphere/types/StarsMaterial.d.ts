import { AtmosphereMaterialBase, AtmosphereMaterialBaseParameters } from './AtmosphereMaterialBase';
import { Vector2, BufferGeometry, Camera, Group, Object3D, Scene, WebGLRenderer } from 'three';

declare module 'three' {
    interface Camera {
        isPerspectiveCamera?: boolean;
    }
}
export interface StarsMaterialParameters extends AtmosphereMaterialBaseParameters {
    pointSize?: number;
    radianceScale?: number;
    background?: boolean;
}
export declare const starsMaterialParametersDefaults: {
    pointSize: number;
    radianceScale: number;
    background: true;
    useHalfFloat: false;
    ellipsoid: import('@takram/three-geospatial').Ellipsoid;
    correctAltitude: true;
    photometric: true;
    renderTargetCount: number;
};
export declare class StarsMaterial extends AtmosphereMaterialBase {
    pointSize: number;
    constructor(params?: StarsMaterialParameters);
    onBeforeRender(renderer: WebGLRenderer, scene: Scene, camera: Camera, geometry: BufferGeometry, object: Object3D, group: Group): void;
    get magnitudeRange(): Vector2;
    get radianceScale(): number;
    set radianceScale(value: number);
    get background(): boolean;
    set background(value: boolean);
}
